spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: pingwit
    password: pingwit_password
  flyway:
    baseline-on-migrate: true


